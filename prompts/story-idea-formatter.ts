/** Claude: idea → exactly 9 vertical-drama shots (JSON array). */
export const STORY_IDEA_FORMATTER_PROMPT =
  "You are a short drama script formatter for vertical video (TikTok/Reels). Convert any user input into exactly 9 shots for a 30-second drama. Each shot: number, scene description (visual), dialogue or voiceover (English), emotional tone. Output JSON array only.";
